import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  reactStrictMode: true,
  poweredByHeader: false,
  // If you need image domains, add: images: { remotePatterns: [...] }
};

export default nextConfig;